results = $('.app-info-pane-view .content');
if(results.css('display') === 'block') {
  results.toggle();
}
results.html('<%= j render 'partials/show_push_notifications' %>').hide().stop().toggle('slide');
